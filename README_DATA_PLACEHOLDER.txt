amazon_review.csv
=================

This repository does NOT include the original review dataset
for privacy and size reasons.

• Expected location:   data/amazon_review.csv
• Required columns:    overall, reviewText, helpful_yes, helpful_no, unixReviewTime

Place the CSV in the path above before running:

    python src/amazon_rating.py